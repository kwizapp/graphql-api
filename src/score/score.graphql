type Query {
  scoreTitleResponse(
    imdbId: String!
    selectedTitle: String!
    remainingSeconds: Int!
  ): Int

  scoreBonusResponse(
    imdbIds: [String!]!
    titleQuestionScores: Int!
  ): Int
}
