type Movie {
  imdbId: String!
  title: String!
  releaseYear: Int!
  posterPath: String!
  randomMovies(num: Int, differentReleaseYear: Boolean): [Movie!]
}

type Query {
  movie(imdbId: String): Movie
}
