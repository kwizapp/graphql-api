type Movie {
  id: ID! # TODO: maybe this can be removed if there is no other id
  imdbId: String!
  title: String!
  releaseYear: Int!
  posterPath: String!
}

type Query {
  movies: [Movie!]!
  movie(imdbId: String!): Movie
}
